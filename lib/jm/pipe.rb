module JM
  # Pipes data from one object into another and back
  class Pipe
    # Pipe data from source to target
    def pipe(source, target)
    end

    # Slurp data from target back to source
    def slurp(source, target)
    end
  end
end
